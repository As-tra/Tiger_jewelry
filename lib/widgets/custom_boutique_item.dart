import 'package:flutter/material.dart';

class CustomBoutiqueItem extends StatelessWidget {
  const CustomBoutiqueItem({super.key});

  @override
  Widget build(BuildContext context) {
    return ListTile(
      
    );
  }
}